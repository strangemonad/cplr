/* tokensets.h */

extern void initialize_set(), clear_set(), merge_sets(),
        print_set(), expand_set();

extern BOOL add_element(), remove_element(),
        test_merge_sets(), member_test(), disjoint();
