% LaTeX style sheet to be used for the CPLR document.

% Set the font to something somewhat less hideous than the grotesque serif
% font used by default in the document class
%
% I go the following from:
% http://www.rpi.edu/dept/arc/training/latex/resumes/helvetica.sty Sets the
% default fonts to helvetica.
\renewcommand{\sfdefault}{phv}
\renewcommand{\rmdefault}{phv}
\renewcommand{\ttdefault}{pcr}
\renewcommand{\ttfamily}{\fontfamily{\ttdefault} \fontsize{8}{10} \selectfont}

\usepackage{color}
\definecolor{DarkBlue}{rgb}{0,0.08,0.45}

\endinput